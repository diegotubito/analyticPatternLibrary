import XCTest
@testable import AnalyticPatternLibrary

final class AnalyticPatternLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AnalyticPatternLibrary().text, "Hello, World!")
    }
}
